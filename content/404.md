---
title: "404 - 页面走失了"
date: 2026-06-28T10:00:00+08:00
draft: false
---

<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
body {
    font-family: 'PingFang SC', 'Microsoft YaHei', sans-serif;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    overflow: hidden;
}
.game-container {
    text-align: center;
    background: white;
    border-radius: 20px;
    padding: 40px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.3);
    max-width: 600px;
    width: 90%;
}
h1 { font-size: 28px; color: #333; margin-bottom: 10px; }
.subtitle { color: #666; margin-bottom: 30px; }
.score-board {
    display: flex;
    justify-content: space-around;
    margin-bottom: 30px;
    font-size: 18px;
}
.score-item {
    background: #f5f5f5;
    padding: 15px 30px;
    border-radius: 10px;
}
.score-item span {
    font-weight: bold;
    font-size: 24px;
    color: #667eea;
}
.game-area {
    position: relative;
    width: 100%;
    height: 300px;
    background: #f0f0f0;
    border-radius: 15px;
    cursor: pointer;
    overflow: hidden;
    margin-bottom: 20px;
}
.target {
    position: absolute;
    font-size: 48px;
    cursor: pointer;
    user-select: none;
    transition: transform 0.1s;
}
.target:hover { transform: scale(1.2); }
.target:active { transform: scale(0.8); }
.btn {
    padding: 12px 30px;
    font-size: 16px;
    border: none;
    border-radius: 8px;
    cursor: pointer;
    margin: 0 10px;
    transition: transform 0.2s;
}
.btn-primary {
    background: #667eea;
    color: white;
}
.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
}
.btn-secondary {
    background: #ddd;
    color: #333;
}
.btn-secondary:hover { transform: translateY(-2px); }
.result-overlay {
    position: absolute;
    top: 0; left: 0; width: 100%; height: 100%;
    background: rgba(0,0,0,0.7);
    display: flex; flex-direction: column;
    align-items: center; justify-content: center;
    border-radius: 15px; color: white;
}
.result-overlay h2 { font-size: 36px; margin-bottom: 10px; }
.result-overlay p { font-size: 24px; margin-bottom: 30px; }
.result-overlay .final-score {
    font-size: 64px; font-weight: bold; color: #ffd700;
}
.hidden { display: none !important; }
.home-link {
    display: inline-block; margin-top: 20px;
    color: #667eea; text-decoration: none; font-weight: bold;
}
.home-link:hover { text-decoration: underline; }
</style>

<div class="game-container">
    <h1>😱 404 - 页面走失了</h1>
    <p class="subtitle">不如玩个游戏放松一下？点击笑脸得分！</p>
    <div class="score-board">
        <div class="score-item">得分: <span id="score">0</span></div>
        <div class="score-item">时间: <span id="time">30</span>秒</div>
    </div>
    <div class="game-area" id="gameArea">
        <div class="target" id="target">😊</div>
        <div class="result-overlay hidden" id="resultOverlay">
            <h2>游戏结束！</h2>
            <p>你的得分是</p>
            <p class="final-score" id="finalScore">0</p>
            <button class="btn btn-primary" onclick="restartGame()">再玩一次</button>
        </div>
    </div>
    <div>
        <button class="btn btn-primary" onclick="startGame()">开始游戏</button>
        <button class="btn btn-secondary" onclick="location.href='/'">返回首页</button>
    </div>
    <a href="/" class="home-link">← 返回首页</a>
</div>

<script>
let score = 0, timeLeft = 30, gameInterval, timeInterval, isPlaying = false, speed = 1;
function startGame() {
    score = 0; timeLeft = 30; speed = 1; isPlaying = true;
    document.getElementById('score').textContent = score;
    document.getElementById('time').textContent = timeLeft;
    document.getElementById('resultOverlay').classList.add('hidden');
    document.getElementById('target').style.display = 'block';
    moveTarget();
    timeInterval = setInterval(() => {
        timeLeft--;
        document.getElementById('time').textContent = timeLeft;
        if (timeLeft === 20) speed = 1.3;
        if (timeLeft === 10) speed = 1.6;
        if (timeLeft <= 0) endGame();
    }, 1000);
}
function endGame() {
    isPlaying = false;
    clearInterval(timeInterval);
    clearTimeout(gameInterval);
    document.getElementById('target').style.display = 'none';
    document.getElementById('finalScore').textContent = score;
    document.getElementById('resultOverlay').classList.remove('hidden');
}
function restartGame() { startGame(); }
function moveTarget() {
    if (!isPlaying) return;
    const target = document.getElementById('target');
    const gameArea = document.getElementById('gameArea');
    const x = Math.random() * (gameArea.offsetWidth - 60);
    const y = Math.random() * (gameArea.offsetHeight - 60);
    target.style.left = x + 'px';
    target.style.top = y + 'px';
    target.textContent = ['😊','😎','🥳','😋','🤩','😜','🤪','😇','🥰','😻'][Math.floor(Math.random()*10)];
    gameInterval = setTimeout(moveTarget, Math.max(300, 1000/speed));
}
document.getElementById('target').addEventListener('click', (e) => {
    e.stopPropagation();
    if (!isPlaying) return;
    score++;
    document.getElementById('score').textContent = score;
    const pop = document.createElement('div');
    pop.textContent = ['💥','✨','🎉','⭐','🔥'][Math.floor(Math.random()*5)];
    pop.style.cssText = `position:absolute;left:${e.offsetX}px;top:${e.offsetY}px;font-size:24px;pointer-events:none;transition:all 0.5s ease-out`;
    document.getElementById('gameArea').appendChild(pop);
    setTimeout(() => { pop.style.opacity = '0'; pop.style.transform = 'translateY(-30px)'; setTimeout(() => pop.remove(), 500); }, 10);
    moveTarget();
});
</script>