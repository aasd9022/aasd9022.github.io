---
title: "博客主题样式操作指南"
date: 2024-06-21T16:00:00+08:00
draft: false
tags: ["PaperMod", "样式", "配置", "指南"]
---

# 博客主题样式操作指南

本文档记录了 PaperMod 主题的文件结构、配置说明和样式修改方法，方便后续维护和自定义。

## 一、核心配置文件

### config.yml - 主配置文件

位置：`f:\my-blog\config.yml`

这是博客的核心配置文件，控制网站的基本设置和功能开关。

#### 基础配置

```yaml
theme: PaperMod              # 主题名称
baseURL: "https://aasd9022.github.io/"  # 网站地址
title: "我的技术博客"         # 网站标题
```

#### 功能开关（params 部分）

| 参数 | 说明 | 默认值 |
|------|------|--------|
| `comments` | 评论系统总开关 | `true` |
| `defaultTheme` | 默认主题（auto/light/dark） | `auto` |
| `ShowToc` | 显示文章目录 | `true` |
| `TocOpen` | 目录默认展开 | `false` |
| `ShowReadingTime` | 显示阅读时长 | `true` |
| `ShowWordCount` | 显示文章字数 | `true` |
| `ShowPostNavLinks` | 上下篇文章导航 | `true` |
| `ShowBreadCrumbs` | 面包屑导航 | `true` |
| `ShowCodeCopyButtons` | 代码块复制按钮 | `true` |
| `ShowLastMod` | 文章最后修改时间 | `true` |
| `VisitCount` | 文章访问统计 | `true` |
| `ShowShareButtons` | 文章分享按钮 | `true` |
| `ShowRelatedPosts` | 相关文章推荐 | `true` |
| `disableThemeToggle` | 禁用主题切换按钮 | `false` |
| `disableScrollToTop` | 禁用返回顶部按钮 | `false` |

#### 首页信息展示

```yaml
homeInfoParams:
  Title: "欢迎来到我的博客"
  Content: "在这里分享技术心得、编程经验和生活感悟"
```

#### 社交图标

```yaml
socialIcons:
  - name: github
    url: "https://github.com/aasd9022"
  - name: twitter
    url: "https://twitter.com/"
  - name: mail
    url: "mailto:"
```

支持的社交图标名称：`github`, `twitter`, `linkedin`, `mail`, `rss`, `youtube`, `instagram` 等。

#### 导航菜单

```yaml
menu:
  main:
    - name: 首页
      url: /
      weight: 1    # 权重决定排序顺序
    - name: 归档
      url: /archives/
      weight: 2
    - name: 标签
      url: /tags/
      weight: 3
    - name: 搜索
      url: /search/
      weight: 4
```

---

## 二、主题 CSS 文件结构

主题样式文件位于：`f:\my-blog\themes\PaperMod\assets\css\`

### 目录结构

```
assets/css/
├── core/               # 核心样式
│   ├── theme-vars.css  # 主题变量（颜色、间距等）
│   ├── reset.css       # 基础重置样式
│   ├── zmedia.css      # 响应式媒体查询
│   └── license.css     # 许可证信息
│
├── common/             # 公共组件样式
│   ├── header.css      # 导航栏样式
│   ├── footer.css      # 页脚样式
│   ├── main.css        # 主内容区样式
│   ├── post-single.css # 文章详情页样式
│   ├── post-entry.css  # 文章列表卡片样式
│   ├── archive.css     # 归档页样式
│   ├── search.css      # 搜索页样式
│   ├── profile-mode.css # 个人资料模式样式
│   ├── md-content.css  # Markdown 内容样式
│   ├── terms.css       # 标签/分类页样式
│   └── 404.css         # 404 页面样式
│
├── includes/           # 代码高亮
│   ├── chroma-styles.css
│   └── chroma-mod.css
│
└── extended/           # 扩展样式（用户自定义）
    └── blank.css
```

---

## 三、关键样式文件详解

### 1. theme-vars.css - 主题变量

位置：`themes/PaperMod/assets/css/core/theme-vars.css`

定义了全局 CSS 变量，控制整个主题的颜色体系。

#### 浅色模式变量

```css
:root {
    --gap: 24px;                    # 间距
    --radius: 12px;                 # 圆角大小
    --theme: rgb(255, 255, 255);    # 背景色
    --entry: rgb(255, 255, 255);    # 卡片背景色
    --primary: rgb(30, 30, 30);     # 主要文字颜色
    --secondary: rgb(108, 108, 108); # 次要文字颜色
    --border: rgb(230, 230, 235);   # 边框颜色
    --accent: rgb(66, 153, 225);    # 强调色（链接、按钮等）
    --gradient-start: rgb(66, 153, 225);  # 渐变起始色
    --gradient-end: rgb(156, 39, 176);    # 渐变结束色
}
```

#### 深色模式变量

```css
:root[data-theme="dark"] {
    --theme: rgb(20, 20, 25);
    --entry: rgb(30, 30, 38);
    --primary: rgb(230, 230, 235);
    --secondary: rgb(140, 140, 150);
    --border: rgb(45, 45, 55);
    --accent: rgb(96, 179, 255);
    --gradient-start: rgb(96, 179, 255);
    --gradient-end: rgb(192, 108, 242);
}
```

#### 如何修改主题色

1. **修改强调色**：找到 `--accent` 变量，修改为你喜欢的颜色
2. **修改渐变色**：修改 `--gradient-start` 和 `--gradient-end`
3. **修改圆角大小**：修改 `--radius` 值（默认 12px）

---

### 2. header.css - 导航栏样式

位置：`themes/PaperMod/assets/css/common/header.css`

控制顶部导航栏的外观。

#### 主要样式类

| 类名 | 说明 |
|------|------|
| `.header` | 导航栏容器，控制毛玻璃效果 |
| `.header-nav` | 导航栏内部布局 |
| `.logo a` | Logo 样式，包含渐变效果 |
| `.menu a` | 导航链接样式 |
| `.menu .active::after` | 当前页面下划线 |
| `.theme-toggle` | 主题切换按钮 |

#### 常见修改

**修改导航栏高度**：
```css
/* 在 theme-vars.css 中修改 */
--header-height: 60px;
```

**修改 Logo 字体大小**：
```css
.logo a {
    font-size: 24px;  /* 修改此值 */
}
```

**修改导航链接悬停效果**：
```css
.menu a:hover::after {
    width: 100%;      /* 下划线展开宽度 */
}
```

---

### 3. post-single.css - 文章详情页

位置：`themes/PaperMod/assets/css/common/post-single.css`

控制单篇文章页面的样式。

#### 主要样式类

| 类名 | 说明 |
|------|------|
| `.post-single` | 文章卡片容器 |
| `.post-title` | 文章标题（渐变效果） |
| `.post-content` | 文章正文区域 |
| `.post-content h2` | 二级标题样式 |
| `.post-content blockquote` | 引用块样式 |
| `.post-tags a` | 标签样式 |
| `.share-buttons` | 分享按钮区域 |
| `.paginav` | 上下篇导航 |

#### 常见修改

**修改文章标题大小**：
```css
.post-title {
    font-size: 40px;  /* 修改此值 */
}
```

**修改引用块样式**：
```css
.post-content blockquote {
    border-left: 4px solid var(--accent);  /* 左边框颜色 */
    background: var(--accent-light);       /* 背景色 */
}
```

**修改文章卡片阴影**：
```css
.post-single {
    box-shadow: 0 4px 20px var(--shadow);  /* 阴影大小 */
}
```

---

### 4. post-entry.css - 文章列表卡片

位置：`themes/PaperMod/assets/css/common/post-entry.css`

控制首页文章列表卡片样式。

#### 主要样式类

| 类名 | 说明 |
|------|------|
| `.first-entry` | 首篇文章特殊样式 |
| `.post-entry` | 普通文章卡片 |
| `.home-info` | 首页信息展示区 |
| `.entry-header h2` | 卡片标题 |
| `.entry-content` | 卡片摘要 |

#### 常见修改

**修改卡片悬停上浮距离**：
```css
.post-entry:hover {
    transform: translateY(-3px);  /* 上浮距离 */
}
```

**修改首篇文章背景渐变**：
```css
.first-entry {
    background: linear-gradient(135deg, 
        rgba(66, 153, 225, 0.1), 
        rgba(156, 39, 176, 0.1));
}
```

---

### 5. footer.css - 页脚样式

位置：`themes/PaperMod/assets/css/common/footer.css`

控制页脚和返回顶部按钮。

#### 主要样式类

| 类名 | 说明 |
|------|------|
| `.footer` | 页脚容器 |
| `.footer a` | 页脚链接 |
| `.top-link` | 返回顶部按钮 |

#### 常见修改

**修改返回顶部按钮颜色**：
```css
.top-link {
    background: var(--accent);  /* 按钮背景色 */
}
```

**修改返回顶部按钮位置**：
```css
.top-link {
    bottom: 2rem;  /* 距底部距离 */
    right: 2rem;   /* 距右侧距离 */
}
```

---

## 四、模板文件结构

模板文件位于：`f:\my-blog\themes\PaperMod\layouts\`

### 主要模板文件

| 文件 | 说明 |
|------|------|
| `baseof.html` | 基础模板框架 |
| `list.html` | 列表页模板（首页、归档等） |
| `single.html` | 单篇文章模板 |
| `archives.html` | 归档页模板 |
| `search.html` | 搜索页模板 |
| `404.html` | 404 页面模板 |

### Partials（局部模板）

位于：`layouts/_partials/`

| 文件 | 说明 |
|------|------|
| `header.html` | 导航栏模板 |
| `footer.html` | 页脚模板 |
| `comments.html` | 评论系统模板 |
| `toc.html` | 目录模板 |
| `post_meta.html` | 文章元信息模板 |
| `share_icons.html` | 分享按钮模板 |
| `social_icons.html` | 社交图标模板 |
| `home_info.html` | 首页信息模板 |

---

## 五、评论系统配置

### Utterances 配置

评论系统模板位置：`themes/PaperMod/layouts/_partials/comments.html`

当前配置：
```html
<script src="https://utteranc.es/client.js"
        repo="aasd9022/aasd9022.github.io"
        issue-term="pathname"
        theme="github-light"
        crossorigin="anonymous"
        defer>
</script>
```

#### 可修改参数

| 参数 | 说明 | 可选值 |
|------|------|--------|
| `repo` | GitHub 仓库 | 你的仓库地址 |
| `issue-term` | Issue 映射方式 | `pathname`, `url`, `title`, `og:title` |
| `theme` | 评论主题 | `github-light`, `github-dark`, `preferred-color-scheme` |

---

## 六、自定义样式扩展

### 方法一：修改主题 CSS 文件

直接修改 `themes/PaperMod/assets/css/` 下的文件。

**优点**：直接生效
**缺点**：主题更新时可能丢失修改

### 方法二：使用扩展样式文件

在 `assets/css/extended/` 目录创建自定义 CSS 文件。

1. 创建文件：`assets/css/extended/custom.css`
2. 添加自定义样式
3. Hugo 会自动合并这些样式

**优点**：不影响主题原文件，便于维护

### 方法三：覆盖模板文件

在项目根目录 `layouts/` 创建同名模板文件，覆盖主题模板。

例如：创建 `layouts/_partials/comments.html` 覆盖主题的评论模板。

---

## 七、常用操作指南

### 修改网站标题和 Logo

1. 打开 `config.yml`
2. 修改 `title` 字段
3. 如需添加 Logo 图片，配置 `params.label.icon`

```yaml
params:
  label:
    text: "Home"
    icon: "/logo.png"
    iconHeight: 35
```

### 添加新文章

1. 在 `content/posts/` 创建新的 `.md` 文件
2. 添加 Front Matter：

```yaml
---
title: "文章标题"
date: 2024-06-21T10:00:00+08:00
draft: false
tags: ["标签1", "标签2"]
categories: ["分类"]
---
```

### 修改文章列表显示数量

在 `config.yml` 添加：

```yaml
paginate: 10  # 每页显示文章数
```

### 添加自定义页面

1. 在 `content/` 创建文件夹（如 `about/`）
2. 创建 `_index.md` 文件
3. 在 `menu.main` 添加导航链接

### 启用/禁用某篇文章的评论

在文章 Front Matter 中设置：

```yaml
comments: false  # 禁用评论
```

### 添加文章封面图片

在文章 Front Matter 中设置：

```yaml
cover:
  image: "images/cover.jpg"
  alt: "封面描述"
  caption: "图片说明"
```

---

## 八、开发调试

### 本地预览

```bash
hugo server --config config.yml --bind 0.0.0.0 --port 1313
```

访问：`http://localhost:1313/`

### 构建生产版本

```bash
hugo --config config.yml
```

生成的文件位于 `public/` 目录。

### 清理缓存

```bash
hugo --gc
```

---

## 九、文件路径快速索引

| 功能 | 文件路径 |
|------|----------|
| 主配置 | `config.yml` |
| 主题变量 | `themes/PaperMod/assets/css/core/theme-vars.css` |
| 导航栏样式 | `themes/PaperMod/assets/css/common/header.css` |
| 文章详情样式 | `themes/PaperMod/assets/css/common/post-single.css` |
| 文章列表样式 | `themes/PaperMod/assets/css/common/post-entry.css` |
| 页脚样式 | `themes/PaperMod/assets/css/common/footer.css` |
| 评论模板 | `themes/PaperMod/layouts/_partials/comments.html` |
| 文章模板 | `themes/PaperMod/layouts/single.html` |
| 首页模板 | `themes/PaperMod/layouts/list.html` |
| 导航模板 | `themes/PaperMod/layouts/_partials/header.html` |
| 自定义扩展 | `assets/css/extended/custom.css` |

---

## 十、常见问题

### Q: 修改 CSS 后样式没有变化？

A: 清理浏览器缓存，或使用 `hugo server --disableFastRender` 强制完整重建。

### Q: 深色模式下颜色不对？

A: 检查是否同时修改了 `:root` 和 `:root[data-theme="dark"]` 两个部分的变量。

### Q: 评论系统不显示？

A: 确保：
1. GitHub 仓库是公开的
2. 已安装 Utterances App
3. 网站已部署到实际域名（本地预览可能不显示）

### Q: 如何添加新的社交图标？

A: 在 `config.yml` 的 `socialIcons` 数组中添加新项，PaperMod 支持的图标名称可参考主题文档。

---

*最后更新：2024-06-21*

---

## 十一、最新修改记录（黑白极简风格改造）

### 设计原则
- **极简黑白风格**：纯黑(#000) + 深灰(#333) + 浅灰(#e5e5e5) 边框
- **统一字体**：`PingFang SC`, `Microsoft YaHei`, `Helvetica Neue`, `Arial`, sans-serif
- **去除所有彩色**：无渐变、无蓝色、无装饰背景
- **无圆角**：所有圆角设置为 0
- **极致留白**：导航、内容、按钮都使用大量留白

### 本次修改的清单

#### 1. config.yml 关键配置
- `defaultTheme: light` - 固定浅色主题
- `disableThemeToggle: true` - 移除主题切换按钮
- `disableScrollToTop: true` - 移除返回顶部按钮
- `label.text: "我的技术博客"` - Logo 改为中文

#### 2. 自定义模板文件

**layouts/list.html** - 首页模板
- 简化结构：Hero 区域 + 文章列表 + 分页
- 文章摘要使用 `truncate 80` 限制字数
- 分页按钮：纯文字 + 箭头（← 上一页 / 下一页 →）

**layouts/_partials/breadcrumbs.html** - 面包屑
- 自定义为：只显示"首页"链接
- 去掉英文 "Home"
- 隐藏分隔箭头

#### 3. 自定义样式文件

**assets/css/extended/custom.css** - 首页自定义样式
- `.hero`：标题 22px、描述 14px、按钮 13px
- `.article-summary`：限制 2 行，高度自动裁剪
- `.article-meta`：12px 日期显示

#### 4. 主题核心样式

**themes/PaperMod/assets/css/core/theme-vars.css**
- 添加 `--base-font` 字体变量
- 添加 `--mono-font` 等宽字体变量
- `*` 选择器统一字体
- 去除深色模式

**themes/PaperMod/assets/css/common/header.css**
- Logo 14px、普通字重
- 菜单项 14px
- 去除 Logo 渐变色

**themes/PaperMod/assets/css/common/post-single.css**
- 文章标题 22px、字重 600
- TOC 边框去除
- TOC 箭头隐藏
- 文章正文字体 14px
- H2 18px、H3 16px、H4 14px

**themes/PaperMod/assets/css/common/post-entry.css**
- 标题 16px、字重 600
- 摘要 13px、限 2 行
- 日期 12px
- 隐藏文章封面

**themes/PaperMod/assets/css/common/footer.css**
- 页脚文字 12px
- 隐藏返回顶部按钮

**themes/PaperMod/assets/css/common/main.css**
- 分页按钮：纯文字 + 箭头
- 去除黑色背景
- 代码块复制按钮：浅灰色边框

### 关于预览报错 (net::ERR_ABORTED)

如果浏览器预览显示 `net::ERR_ABORTED` 错误：
- 这是**浏览器/插件**问题，不是网站问题
- 实际服务器正常运行
- **解决方法**：在浏览器中**手动打开** http://localhost:1313/
- 或尝试使用**无痕模式**访问

### 常用调试命令

```bash
# 启动开发服务器
hugo server --config config.yml --bind 0.0.0.0 --port 1313

# 强制完整重建
hugo server --config config.yml --disableFastRender

# 构建生产版本
hugo --config config.yml

# 清理缓存
hugo --gc
```

---

## 十二、操作日志（按时间倒序）

> 这里记录每次对博客的修改内容，方便回溯和查阅。
> 最新修改在顶部，最早修改在底部。

### 📅 2024-06-22 第六次修改 - Git 配置和文档更新

**用户反馈**：
- git status 显示 public/ 目录被跟踪
- 需要更新操作手册

**修改内容**：

| 修改项 | 详情 |
|--------|------|
| 创建 .gitignore | 忽略 public/、resources/、.hugo_build.lock |
| 移除 public/ 跟踪 | 使用 `git rm -r --cached public/` |
| 更新 Git 手册 | 添加 .gitignore 章节、子模块章节 |
| 更新操作指南 | 添加本次修改记录 |

**涉及文件**：
- `.gitignore` - 新建
- `content/posts/Git操作手册.md` - 更新
- `content/posts/操作指南.md` - 更新

---

### 📅 2024-06-22 第七次修改 - 深色模式支持

**用户反馈**：
- 切换到黑色模式后，文字还是黑色看不见
- 需要深色模式下文字变成白色

**修改内容**：

| 修改项 | 详情 |
|--------|------|
| 背景色 | 深色模式使用 #0a0a0a 纯黑背景 |
| 主文字色 | 深色模式使用 #e5e5e5 浅灰白 |
| 次要文字 | 深色模式使用 #999 中灰色 |
| 边框色 | 深色模式使用 #333 深灰 |
| 代码块背景 | 深色模式使用 #1a1a1a 深色背景 |

**涉及文件**：
- `themes/PaperMod/assets/css/core/theme-vars.css` - 添加深色模式变量
- `themes/PaperMod/assets/css/common/header.css` - 添加深色模式
- `themes/PaperMod/assets/css/common/post-single.css` - 添加深色模式
- `themes/PaperMod/assets/css/common/post-entry.css` - 添加深色模式
- `themes/PaperMod/assets/css/common/main.css` - 添加深色模式
- `themes/PaperMod/assets/css/common/footer.css` - 添加深色模式
- `assets/css/extended/custom.css` - 添加深色模式

---

### 📅 2024-06-21 第五次修改 - 风格细节优化

**用户反馈**：
- 摘要文字太多，限制字数
- 字体不统一（标题、正文、按钮）
- 顶部出现两个"首页"（英文 + 中文重复）
- 面包屑出现英文 + 灰色框
- TOC 展开箭头太大
- 报错 net::ERR_ABORTED

**修改内容**：

| 修改项 | 详情 |
|--------|------|
| 摘要字数限制 | 使用 `truncate 80` + CSS `-webkit-line-clamp: 2` |
| 字体统一 | 引入 `--base-font` 变量：`PingFang SC`, `Microsoft YaHei` |
| 标题字体 | 22px / 600 字重 |
| 正文字体 | 14px / 400 字重 |
| 按钮字体 | 13px / 400 字重 |
| 代码字体 | 等宽字体 `--mono-font` |
| Logo 文字 | 英文 "Home" → 中文 "我的技术博客" |
| 面包屑 | 自定义为只显示"首页"，去掉英文 |
| TOC 边框 | 去除灰色边框 |
| TOC 箭头 | 隐藏默认箭头 |
| 分页按钮 | 纯文字 + 箭头，去除黑色背景 |

**涉及文件**：
- `layouts/list.html` - 摘要加 `truncate 80`
- `layouts/_partials/breadcrumbs.html` - 自定义面包屑
- `assets/css/extended/custom.css` - 摘要截行
- `themes/PaperMod/assets/css/core/theme-vars.css` - 字体变量
- `themes/PaperMod/assets/css/common/header.css` - 字体大小
- `themes/PaperMod/assets/css/common/post-single.css` - 文章样式
- `themes/PaperMod/assets/css/common/post-entry.css` - 列表样式
- `themes/PaperMod/assets/css/common/footer.css` - 页脚样式
- `themes/PaperMod/assets/css/common/main.css` - 分页按钮

---

### 📅 2024-06-21 第四次修改 - 黑白极简风格

**用户反馈**：
- 社交图标不需要
- 页面布局不合理
- 仿照 Trae 官网风格（但保持博客功能）
- **再次强调**：黑白简约风、单色灰度、极致留白、高对比度、几何线条、0 冗余元素、克制美学、性冷淡、高级质感

**修改内容**：

| 修改项 | 详情 |
|--------|------|
| 去除社交图标 | 删除 `socialIcons` 配置 |
| 自定义首页 | 创建 `layouts/list.html` |
| 配色 | 纯黑(#000) + 深灰(#333) + 浅灰(#999) + 边框(#e5e5e5) |
| 渐变色 | 全部去除 |
| 圆角 | 全部设为 0 |
| 阴影 | 全部去除 |
| 装饰背景 | 全部去除 |
| 文章卡片 | 改为纯文字列表，无背景无边框 |
| 主题切换 | 禁用 (`disableThemeToggle: true`) |
| 返回顶部 | 禁用 (`disableScrollToTop: true`) |
| 页脚文字 | 留空 |

**涉及文件**：
- `config.yml` - 删除社交图标
- `layouts/list.html` - 新建首页模板
- `assets/css/extended/custom.css` - 新建自定义样式
- `themes/PaperMod/assets/css/core/theme-vars.css` - 配色变量
- `themes/PaperMod/assets/css/common/*.css` - 所有公共样式重写

---

### 📅 2024-06-21 第三次修改 - 仿照 Trae 官网

**用户反馈**：
- 仿照 Trae 官网风格
- **但要保持博客功能**，不要加其他功能

**修改内容**：
- 创建 Hero 区域（大标题 + 简介 + 按钮）
- 添加特性卡片（技术笔记、问题解决、学习分享）
- 改为文章卡片网格布局
- 添加渐变色（蓝色到紫色）

**涉及文件**：
- `layouts/list.html` - 新建首页模板
- `assets/css/extended/custom.css` - 新建样式

---

### 📅 2024-06-21 第二次修改 - 增强配置和样式

**用户反馈**：
- 看看主题还有什么自带功能
- 样式单调，需要重新设计

**修改内容**：
- 启用社交图标
- 添加首页信息展示
- 启用相关文章推荐
- 启用编辑文章链接
- 添加完整导航菜单
- 增强所有 CSS 样式：渐变、阴影、动画、毛玻璃效果
- 修改主题变量添加渐变色
- 重写 header、post-single、post-entry、footer 样式

**涉及文件**：
- `config.yml` - 启用多个新功能
- `themes/PaperMod/assets/css/core/theme-vars.css` - 渐变变量
- `themes/PaperMod/assets/css/common/header.css` - 毛玻璃导航
- `themes/PaperMod/assets/css/common/post-single.css` - 文章样式
- `themes/PaperMod/assets/css/common/post-entry.css` - 卡片样式
- `themes/PaperMod/assets/css/common/footer.css` - 页脚样式

---

### 📅 2024-06-21 第一次修改 - 修复 Utterances 评论系统

**用户反馈**：
- 添加了 Utterances 评论插件但不显示

**问题原因**：
- PaperMod 主题的 `comments.html` 是空占位符
- 文章中直接嵌入的 HTML 代码无法正常显示

**修改内容**：
- 修改 `themes/PaperMod/layouts/_partials/comments.html` 添加 Utterances 代码
- 清理文章中重复的评论代码

**涉及文件**：
- `themes/PaperMod/layouts/_partials/comments.html` - 添加评论代码
- `content/posts/第一篇Hugo博客.md` - 清理重复代码
- `config.yml` - 评论系统配置

---

## 十三、修改模板（复制这个模板）

以后每次修改，按照下面的格式在"操作日志"章节顶部添加：

```markdown
### 📅 YYYY-MM-DD 第N次修改 - 简短描述

**用户反馈**：
- 用户提出的问题或需求

**修改内容**：

| 修改项 | 详情 |
|--------|------|
| xxx | xxx |

**涉及文件**：
- 文件路径 1
- 文件路径 2
```